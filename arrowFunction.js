/**
 * arrow function need to store in a variable to execute.
const functionName = (param1, param2..) => {
}
 */


// sum function 

const sumFunction = (num1, num2) => {
    // console.log(`The summation is ${num1 + num2}`); // When not use return
    let result = num1 + num2
    return result;
}

// sumFunction(5, 7)

console.log(sumFunction(8, 9)) // When use return.